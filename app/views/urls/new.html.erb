<h1>New URL</h1>

<%= form_for :url, url: urls_path do |f| %>
  <p>
    <%= f.label :full_url %>
    <%= f.text_field :full_url %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>

<%= link_to 'Back', urls_path %>
